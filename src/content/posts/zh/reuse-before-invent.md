---
title: "先复用，再造：第二个 Agent 九周上线，靠的是现成的基础设施"
description: "第二个 Agent 九周就发出去了，因为它几乎没有一样基础设施是自己的。六次我们都选择住进已有的系统当租户，而不是在旁边再造一套。这么做换来了什么、付出了什么、什么时候该自己造。"
date: 2026-09-27
tags: [engineering, agents]
cover: /img/reuse/peer-vs-tenant.zh.svg
---

先说结论：新产品架构图上的每一个框，先问一句"公司里谁已经在跑一个"。答案是有的，就搬进去住，守它的规矩，只写让它属于你的那部分文字。第二个 Agent 九周上线，靠的就是六次都这么选。

## 开工时的清单

第二个 Agent 开工那天，我列了它需要的东西：能访问工单系统、wiki 和代码托管；指令要有地方放；要能发给别人用；要能开 PR；要能读团队的测试管理工具和覆盖率数据；生成的测试要有地方落。

六项。每一项公司里都已经有人在维护一个。问题只是：给新产品单独造一套、边界干干净净，还是搬进已有的那套、按别人的规矩来。

我们搬了六次。九周上线。换另一条路，我不觉得二十周能上。

## 白板上，"另起一套"更好看

给新产品画架构，最有诱惑力的画法是自己另起一列框：自己的集成层、自己的仓库、自己的发版流水线，整整齐齐立在已有系统旁边。每个框都归你管，你做什么都不会碰坏别人。

但每个框也都是你要跑的一个系统。要凭据、要监控、坏了要有人接、底下的东西一动就要迁移。而且第二列和第一列是重复的，任何改进要么做两遍，要么不做。

<figure>
<a href="/img/reuse/peer-vs-tenant.zh.svg" target="_blank" rel="noopener"><img src="/img/reuse/peer-vs-tenant.zh.svg" alt="左边：新产品画成对等系统，自己一列六个框，复制了已有的网关、技能库、发版流水线、PR 工具、测试工具集成和自动化仓库。右边：同一个产品画成租户，一个薄薄的指令框插进六个已有系统。租户真正拥有的只是一个文本文件夹。"></a>
<figcaption>另起一套要养六个系统。租户只拥有一个文本文件夹，插进六个已经在跑的系统里。</figcaption>
</figure>

## 六次当租户

| Agent 需要 | 没造的 | 搬进了 | 实际写的 |
|---|---|---|---|
| 访问工单、wiki、代码 | 自己的连接器 | 第一个 Agent 已经在用的集成网关，MCP 暴露 | 指令里的工具名，别的没有 |
| 放指令的地方 | 自己的仓库 | 共享技能库，和其他 Agent 的技能同一个目录 | 几份 markdown |
| 发布 | 自己的流水线 | 一个已有插件和它的 marketplace，作为多出来的一组命令 | 升一个版本号，写一条 changelog |
| 开 PR | 一个 PR 步骤 | 插件里已有的建 PR 技能，整个委托 | 零行 |
| 测试用例和覆盖率 | 自己的存储 | 团队的测试管理工具，以及它前面的那个服务 | 什么时候调哪个工具的说明 |
| 生成的测试落地 | 新仓库 | 团队已经在维护的两个自动化仓库 | 按那两个仓库规范写的测试 |

第四行我最喜欢。让 Agent 开一个 PR 听着小，其实不小：分支命名、识别远端、找 reviewer、扫密钥、API 拒绝时的手动兜底。插件里已经有一个技能把这些全做了。我们告诉 Agent 去调它。这就是全部实现。

第二行有个额外收获。技能放在共享库里，另一个跑在完全不同 runtime 上的 Agent 产品能读到同一批文件。我们埋了一个 canary 验证：在某个技能里加一句话，第二天两个产品都能看到。复用不是 PPT 上的一页，是一个 diff。

## 当租户的代价

租户守的是房东的规矩，不是自己的。

改一个技能要两个 MR：一个进共享库，一个把副本同步进插件，然后发版。我们的脚本要能跑在宿主承诺支持的最老 Python 上，比我笔记本上的老，这件事是发版当晚才发现的。命名要按宿主的规范。发版节奏是宿主的。这个月 marketplace 换了家，我们跟着搬，通知用户重装。

这些都不是抱怨，是房租。另一条路是自己养六个系统，而上面每一项成本，跟自己跑一个网关比都是小钱。

## 换来了什么

从第一张工单到别的团队能安装的版本，九周。两个人。几乎没有属于自己的、会半夜报警的基础设施。

还有一件没料到的事：因为我们拥有的东西全是文本，Agent 在几个家之间搬来搬去几乎没动过。插件换 marketplace 那次，指令一个字没改，用户拿到的是四行命令。拥有得少，和可迁移，原来是同一件事。

## 什么时候还是得自己造

复用是默认，不是法律。三种情况让我选择自己造。

房东说不，或者答应得不够快。一个你不能按自己节奏改的依赖，迟早会被你绕开；不如一开始就有意识地做这个决定。

你的用量会伤到宿主。一个夜间任务猛砸一个按交互式流量设计的网关，那不是复用，是好心的 DoS。

抽象跟你打了三次架。我们有一个 bug 两个月里出现三次，每次看着都不相关，直到有人给它起了名字：共享组件里一条兜底路径，对我们的场景来说形状悄悄是错的。修法是上游把兜底修一次，而不是下游再加第四个特例。有时上游修不了，那就自己造。但先数到三。

<div class="callout">
<p><strong>规则：</strong>新产品架构图上的每一个框，先问谁已经在跑一个。搬进去，守它的规矩，只写让它属于你的那些文字。只有房东说不、你的负载会伤到它、或者抽象跟你打了三次架，才自己另起一套。</p>
</div>
